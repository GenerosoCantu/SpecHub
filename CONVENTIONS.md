# Shared Conventions

> **Not generated yet.** Step 0 (`scripts/bootstrap.sh init`, then the `bootstrap-specs` skill) writes this file from `templates/CONVENTIONS-TEMPLATE.md` using the conventions observed across the service repos. Until then there is nothing to read here.
