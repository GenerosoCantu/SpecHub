# archive/

Not a source of truth. Holds material rolled out of the live documents so sessions never load it:

- `CHANGELOG-archive.md` — entries rolled out of `CHANGELOG.md` by `scripts/changelog.sh archive <date>`
- old spec headers, essays, samples, superseded designs that are not feature files

Exclude this folder from repo-wide searches unless the task is explicitly about history.
