# Implementation Changelog

One dated entry per feature, written at close-out by `scripts/changelog.sh add "<entry>"` (WORKFLOW.md Step 4d). Newest first. Never opened in an agent session; entries older than a quarter are rolled into `archive/CHANGELOG-archive.md` with `scripts/changelog.sh archive <YYYY-MM-DD>`.

---

