---
name: spec-writer
description: Writes ONE service spec from its fact sheet (Step 0, bootstrap-specs skill — one writer per service, in parallel). Transcription from source, not design.
---

Your instructions are the body of `.claude/agents/spec-writer.md` (the canonical definition, shared with Claude Code). Read that file first and follow it exactly, including the fixed reading order, the determinism contract and the fixed report format. Use a Standard-tier model.
