# {service-name} — Agent Instructions

> Canonical copy lives in the spec hub at `repo-instructions/{service}.md`. Do not edit this file in the repo; edit the hub copy and re-copy (WORKFLOW.md Step 4g). This file is `AGENTS.md`; `CLAUDE.md` imports it with `@AGENTS.md`.

## Stack

{Language / framework + version / datastore / auth — one line each}

## Conventions

- {ID strategy, e.g. UUIDs via the runtime's crypto module — never a second library}
- {Schema/entity rules, e.g. timestamps on, version key off, tenant field indexed}
- {Guard / auth placement}
- {Error handling pattern and error envelope}
- {Test placement and naming}

## Module structure

```text
{source root}/{feature}/{feature}.module.*
{source root}/{feature}/{feature}.service.*
{source root}/{feature}/{feature}.controller.* | routes.*
{source root}/{feature}/{model|schema|entity}.*
{source root}/{feature}/dto/*
```

## Commands

- Build: `{command}`
- Test: `{command}`
- Lint changed files only: `{command} <files>` — never a repo-wide `--fix`
- Dev server: `{command}` (port {port})

## Environment

{Env var names only — values come from the environment, never hardcoded}

## Where the truth lives

This repo is an implementation target. Contracts, schemas, and endpoint definitions live in the spec hub; implementation tasks arrive as self-contained prompt files. Do not treat this codebase as a source of truth for platform behavior.
